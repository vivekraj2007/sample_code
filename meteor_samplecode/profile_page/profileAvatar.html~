<Template name="profileAvatar">
    <div id="right">
    <div class="show-left-menu-btn"></div>
    <div class="show-right-menu-btn"></div>
    <div class="right-inner">
        {{> gear_menu_context_menu }}
        {{> profile_avatars }}
    </div></div>
    {{> right_menu_container }}
</Template>

<Template name="profile_avatars">
    <div id="main-container">
        <form id="contact_profile" onsubmit='return false;'>
            <div class="title-container container" id="title-container">
                <div class="title-textbox-container">
                    <input type="text" disabled='disabled' class="title-textbox profile-name static-txt" value="{{ contact_name }}"/>
                    {{#if my_username}}
                    <br>
                    <span class='static-txt'>({{ my_username }})</span>
                    {{/ if}}
                </div>
                <ul class="blue-icons no-bar">
                    <li class="talk"><span>ADD</span></li>
                </ul>
                <div class="clear"></div>
            </div>
`                               
            <div id="popup-container" class="container">
                {{> compose_popup }}
            </div>
            <div class="message-container container">
                <div class="message profile-item">
                    <div class="message-body">
                        {{> profile_gravatars }}
                        {{> profile_photo_upload }}
                        <div class="clear"></div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</Template>

<Template name="profile_gravatars">
    {{#each gravatars}}
        <div>
            <label>
                <input type="radio" name="profile_avatar" value="{{email}}" {{#if is_checked}}checked{{/if}}>
                <img src="{{path}}"  class="profile-avatar">
            </label>
        </div>
    {{/each}}

</Template>


<Template name="profile_photo_upload">
    {{#if photo}}
    <div>
        <label>
            <input type="radio" name="profile_avatar" value="{{photo}}" {{#if is_checked}}checked{{/if}}>
            <img src="{{photo}}"  class="profile-avatar">
        </label>
    </div>
    {{/if}}
    <div class="upload-photo-btn-container">
        <input type="file" class="upload-photo-btn transparent">
    </div>
</Template>
